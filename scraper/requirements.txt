# RNTell Scraper — Python dependencies
# Install with: pip install -r requirements.txt
# Then: playwright install chromium

playwright==1.51.0          # Headless browser automation (JS-heavy career sites)
supabase==2.10.0            # Supabase Python client
python-dotenv==1.0.1        # Load .env.local variables
extruct==0.17.0             # JSON-LD / microdata extraction from HTML
w3lib==2.2.1                # URL normalization utilities
lxml==5.3.0                 # Fast HTML parser (used by extruct)
httpx==0.27.2               # Async HTTP client (for non-JS sites)
pydantic==2.10.6            # Schema validation + normalization
rapidfuzz==3.11.0           # Fuzzy string matching for facility dedup
python-dateutil==2.9.0      # Date string normalization
tenacity==9.0.0             # Retry logic for flaky network requests
structlog==24.4.0           # Structured logging
asyncio-throttle==1.0.2     # Rate limiting for polite scraping
